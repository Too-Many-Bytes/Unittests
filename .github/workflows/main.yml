name: Run_tests

on:

  push:
    branches: [ main ]
    
  workflow_dispatch:
  
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run tests for calculating_program
        run: python -m unittest discover . -pattern='test_func_answers.py'
      - uses: yanzay/notify-telegram@v0.1.0
        if: always()
        with:
          chat: 933027728 # user id or channel name secret
          token: 5051639728:AAFf69jhBUdCKFArmpclxyRiocyrXpwZozQ # token secret
          status: ${{ job.status }} # do not modify this line
        
